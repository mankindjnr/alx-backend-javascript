export default function divideFunction(numerator, denominator) {
  try {
    const answ_1 = numerator / denominator;
    if (denominator === 0) {
      throw new Error();
    }
    return answ_1;
  } catch (err) {
    throw Error('cannot divide by 0');
  }
}
